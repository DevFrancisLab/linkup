{
  "name": "linkup",
  "private": true,
  "scripts": {
    "dev": "cd frontend && bun install --frozen-lockfile && bun run dev",
    "build": "bun run --cwd frontend build",
    "lint": "bun run --cwd frontend lint"
  }
}
